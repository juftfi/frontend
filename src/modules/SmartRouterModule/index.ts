import * as SmartRouterHooks from "./hooks";
import * as SmartRouterComponents from "./components";

const SmartRouterModule = {
    hooks: SmartRouterHooks,
    components: SmartRouterComponents,
};

export default SmartRouterModule;
